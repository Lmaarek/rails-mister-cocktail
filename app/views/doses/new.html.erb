<%= simple_form_for [@cocktail, @dose] do |f| %>
  <p><%= f.association :ingredient, prompt: "Select an ingredient" %></p>
  <p><%= f.input :description %></p>
  <p><%= f.submit "Add a dose" %></p>
<% end %>
